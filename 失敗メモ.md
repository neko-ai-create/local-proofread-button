# 失敗メモ

## 2026-09-14 表示された校正ボタンが消える

- 何をした: 登録済み0.2.0の入力欄をクリックした後、ページ内を少しスクロールした。
- 何が起きた: 緑のボタンの要素が削除された。
- 原因: documentのscrollイベントで無条件にcloseを呼んでいた。空き位置の候補がない時も要素を削除していた。
- 今後の対策: スクロールでは位置更新を行う。ブラウザ上の有効版を確認し、表示とスクロール後の状態を実画面で検証する。設定ファイルの検索結果だけで未登録と断定しない。

## 2026-09-17 編集ツールの参照先不一致

- 何をした: 存在を確認した対象ファイルに標準のパッチ編集を行った。
- 何が起きた: ファイルが見つからないというエラーになった。既定の作業ディレクトリも存在しなかった。
- 原因: 編集ツールの実行環境との不一致と考えられるが、詳細は未確認。
- 今後の対策: 存在する作業ディレクトリを明示し、ローカルで確認済みのapply_patch実行パスを指定して編集する。この方法で保存できた。

## 2026-09-17 移動テストの初回失敗

- 何をした: 独立したChromeで移動と画面縮小をテストした。
- 何が起きた: 制限環境ではブラウザが起動できず、権限付きの実行へ切り替えた。画面縮小時の位置検査も失敗した。
- 原因: 起動制限。位置検査はボタン文字の折り返しにより幅が変化し、画面端の計算がずれた。
- 今後の対策: 一時プロファイルでテストする。ボタンにwhite-space:nowrapを設定し、再配置を待って検証する。修正後は全テスト成功。
